import React from 'react';
import PropTypes from 'prop-types';

NarBar.propTypes = {
    
};

function NarBar(props) {
    return (
        <div>
            
        </div>
    );
}

export default NarBar;